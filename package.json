{
  "name": "electron-tasks-app",
  "version": "1.0.0",
  "description": "A minimal Electron application of tasks",
  "main": "main.js",
  "scripts": {
    "start": "npm run electron-dev",
    "electron-dev": "concurrently \"npm run start-react\" \"npm run start-electron-react\"",
    "start-react": "cd listado && cross-env BROWSER=none npm start",
    "start-electron-react": "wait-on http://localhost:3000 && nodemon --exec npm run start-electron",
    "start-electron": "electron ."
  },
  "repository": "https://github.com/profeEsteban/electron-tasks-app",
  "author": "profeEsteban",
  "devDependencies": {
    "electron": "^18.2.4"
  },
  "dependencies": {
    "mongoose": "^6.4.1",
    "nodemon": "^2.0.19",
    "concurrently": "^7.4.0",
    "cross-env": "^7.0.3",
    "wait-on": "^6.0.1"
  }
}
