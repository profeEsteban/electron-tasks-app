{
  "name": "electron-tasks-app",
  "version": "1.0.0",
  "description": "A minimal Electron application of tasks",
  "main": "main.js",
  "scripts": {
    "start": "npm run ia && npm run electron-dev",
    "ia": "concurrently \"npm i\" \"cd listado && npm i\"",
    "electron-dev": "concurrently \"cd listado && cross-env BROWSER=none npm start\" \"wait-on http://localhost:3000 && nodemon --exec npm run start-electron\"",
    "start-electron": "electron ."
  },
  "repository": "https://github.com/profeEsteban/electron-tasks-app",
  "author": "profeEsteban",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "electron": "^18.2.4",
    "nodemon": "^2.0.19"
  },
  "dependencies": {
    "concurrently": "^7.4.0",
    "electron-reload": "^2.0.0-alpha.1",
    "mongoose": "^6.4.1",
    "wait-on": "^6.0.1"
  }
}
